{
  "name": "config-finder",
  "version": "1.0.2-beta.4",
  "description": "Autoload config for a specific module",
  "keywords": [
    "rc",
    "config",
    "finder",
    "package"
  ],
  "author": {
    "name": "allex",
    "email": "allex.wxn@gmail.com"
  },
  "main": "lib/index.js",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git@github.com:allex/config-finder.git"
  },
  "scripts": {
    "build": "babel -d lib src/",
    "watch": "babel -w -d lib src/",
    "lint": "standard",
    "lint:fix": "standard --fix",
    "test": "npm run lint",
    "prepublishOnly": "npm test && npm run build"
  },
  "dependencies": {
    "cosmiconfig": "^3.1.0"
  },
  "devDependencies": {
    "@fedor/standard": "latest",
    "babel-core": "^6.25.0",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.5.2",
    "babel-runtime": "^6.26.0"
  },
  "engines": {
    "node": ">=4"
  }
}
